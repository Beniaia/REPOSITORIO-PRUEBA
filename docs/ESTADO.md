# ESTADO.md — Dónde está el proyecto

> **Este es el documento vivo.** Se actualiza al final de cada sesión de trabajo, tanto si trabajas con Claude Code como si tocas algo a mano.
> Última actualización: **27 agosto 2026** · Fase actual: **F0 y F1 completas. P-15 (detección automática de respuesta) con código desplegado en producción (cron de Vercel + IMAP, D-29/D-30), pendiente de una prueba real de punta a punta. Añadida la página `/campanas` con "prospectar ahora" / "programar para cuando yo quiera" (D-31), sustituyendo la idea de una tarea programada semanal fija. La primera tanda real del motor (10 leads de Levante) sigue deliberadamente para el final — Nuria prefiere seguir probando con leads ficticios primero**

---

## 1. Semáforo

| Bloque | Estado |
|---|---|
| Documentación (PRD, arquitectura, CLAUDE.md) | ✅ Hecho |
| Esquema de base de datos | ✅ Aplicado en Supabase (v1.1: + reuniones, permiso de llamada, guardarraíles de envío como triggers) |
| Proyecto Supabase creado | ✅ `PROSPECCION CLAUDE` (`ytqjhsbcnswshybhgevc`, eu-central-1), 11 tablas con RLS, 0 avisos de seguridad |
| Repo Next.js inicializado | ✅ Next 16 + React 19 + Tailwind + `@supabase/ssr`. `build`, `lint` y `typecheck` en verde |
| Endpoint de ingesta | ✅ `/api/ingest/leads` con validación Zod, dedup por dominio/email, consulta de bajas antes de insertar. Sólo crea borradores, nunca marca enviado |
| Consola de leads | 🟡 Bandeja (`/leads`) y ficha (`/leads/[id]`) funcionando contra Supabase real, con login. Falta pipeline/métricas/ajustes editables |
| Motor de prospección (skill) | ✅ Reescrita para este sistema en `.claude/skills/prospeccion-baladre/SKILL.md` (la versión antigua, que escribía en Sheets, sigue en `agente-prospeccion-baladre/` sin tocar). Sin ejecutar todavía: bloqueada por token de Apify y por que la app esté desplegada |
| Pipeline de Apify | 🟡 Diseñado y **esquemas de entrada validados el 23/08/2026** contra los 3 actores reales (se corrigió un error en Website Content Crawler). Sin ejecutar todavía |
| Descubrimiento con `vibe-prospecting` | ✅ Conector conectado y **cobertura verificada el 22/08/2026** con consultas reales. Recetas en `VIBE_PROSPECTING.md` |
| Tarea programada semanal | ⬜ **Sustituida por D-31**: no habrá cron con fecha fija. Página `/campanas` con botón "prospectar ahora" y formulario "programar para cuando yo quiera", que escriben en la tabla `solicitudes_prospeccion`; la skill del motor la consulta al arrancar. **D-32 (27/08/2026, confirmado con Nuria): la ejecución sigue exigiendo abrir Claude Code y pedirlo** — investigado a fondo si se podía automatizar del todo con una rutina de Claude y descartado: los créditos de `vibe-prospecting` sólo son accesibles desde una conversación viva, no desde una rutina desatendida |
| Redacción y aprobación de emails | ⬜ Pendiente |
| Detección automática de respuesta (P-15) | 🟡 **27/08/2026: código escrito, sin desplegar ni probar.** `lib/gmail.ts` (lectura por **IMAP**, D-30 — misma cuenta y contraseña de aplicación que ya usa el envío por SMTP, sin Google Cloud ni facturación de por medio), `lib/respuestas/registrar-respuesta.ts` (lógica compartida con el registro manual), endpoint `/api/deteccion-respuestas` (GET pendientes / POST registrar, mismo `INGEST_SERVICE_TOKEN`) y cron de Vercel `/api/cron/deteccion-respuestas` (`vercel.json`, diario a las 07:00 UTC). Falta: comprobar que IMAP está habilitado en la cuenta de Gmail piloto, fijar `CRON_SECRET` en `.env.local`/Vercel, y una prueba real de punta a punta |
| Envío real (fase 4, con dominio propio y Resend) | ⛔ Bloqueado por revisión jurídica y por el dominio |
| Envío real — piloto por Gmail personal (D-22) | ✅ Probado con un envío real, funciona |
| Migración del Google Sheets `BALADRE_LEADS` | ✅ No hace falta — los datos son ficticios (confirmado 23/08/2026) |
| Despliegue en Vercel | ✅ **`repositorio-prueba-ylar`** (equipo `nuria-pruebas`), conectado a `Beniaia/REPOSITORIO-PRUEBA`, rama `main`. URL pública: **repositorio-prueba-ylar.vercel.app**. `/login` verificado en producción, sin errores de runtime |
| `.env.local` completo | ✅ 14 variables rellenas: Supabase (URL, anon, service_role), `INGEST_SERVICE_TOKEN`, Gmail, Apify, Zoom (3), Google Calendar (3) |
| Flujo de reunión automática (Zoom + Calendar) | ✅ **26/08/2026: probado de punta a punta en producción con el lead de prueba `nuritina10@hotmail.com`.** Permiso de llamada → aprobar email → Contactar (envío real) → registrar respuesta → confirmar fecha → reunión de Zoom real, evento de Calendar real, invitación automática enviada — todo verificado en Supabase (`reuniones.estado='confirmada'`, `zoom_meeting_id`, `calendar_event_id`, `mensajes.enviado_automaticamente=true`). Bloqueante encontrado y resuelto durante la prueba: faltaban las 6 variables de Zoom/Google Calendar en el entorno de producción de Vercel (sólo estaban en `.env.local`) — añadidas y redeployado. Bug encontrado y corregido en la misma sesión: `confirmarFechaReunion` no era idempotente — un doble envío del formulario creaba una segunda reunión de Zoom, un segundo evento de Calendar y un segundo email de invitación real; ahora comprueba si la reunión ya está `confirmada` antes de crear nada. Detección automática de respuesta (P-15) sigue sin construir — de momento el registro de la respuesta es manual desde la ficha |

## 2. Decisiones tomadas

| # | Decisión | Fecha | Nota |
|---|---|---|---|
| D-01 | Se construye desde cero; no se extiende Relevance AI + Google Sheets | 21/08/2026 | El sistema anterior sigue vivo hasta que este lo sustituya |
| D-02 | Base de datos: Supabase | 21/08/2026 | |
| D-03 | App: Next.js + Vercel, construida con Claude Code | 21/08/2026 | |
| D-04 | El motor de búsqueda vive en Claude; la app muestra y aprueba | 21/08/2026 | La tarea semanal automática se resuelve con una **tarea programada de Claude**, no con un cron de Vercel. Así se cumplen a la vez "la app sólo muestra" y "automático semanal" |
| D-05 | Operativa: automático semanal + bajo demanda | 21/08/2026 | |
| D-06 | Envío desde la app con botón de aprobar (fase 2) | 21/08/2026 | |
| D-07 | Prioridad: calidad del lead sobre volumen. Levante primero, luego resto de España | 21/08/2026 | |
| D-08 | Presupuesto tope ~50 €/mes | 21/08/2026 | |
| D-09 | Un solo cliente (Baladre). Sin multi-tenant | 21/08/2026 | El esquema no lo impide si algún día cambia |
| D-10 | **Apify es el motor de enriquecimiento** de todo lead, ejecutado desde el motor en Claude | 21/08/2026 | Playbook completo en `APIFY.md`. Cuenta ya existente. Confirmado y acotado por D-16 |
| D-11 | LinkedIn **no** entra en el pipeline automatizado | 21/08/2026 | Sus condiciones prohíben el scraping. Alternativas en `APIFY.md` §8. Si Baladre decide lo contrario, ver P-08 |
| D-12 | **Economía de contexto como requisito, no como consejo** | 21/08/2026 | Campos seleccionados en Apify, filtro con código antes del modelo, truncado de webs, scoring por lotes, investigación en subagentes. Reglas en `APIFY.md` §9 y `CLAUDE.md` §11. El prompt maestro pasa a v2: un prompt corto por hito |
| D-13 | ~~Google Maps se limita a hoteles y restauración~~ | 21/08/2026 | **Superada por D-15 al día siguiente**, cuando se pudo medir la cobertura real de `vibe-prospecting` |
| D-14 | **`vibe-prospecting` es el canal de descubrimiento** de todos los segmentos | 22/08/2026 | Conector conectado. Cobertura medida con consultas reales: 1.043 estudios pequeños en Levante; decisores **con email**: 75 en arquitectura Levante, 96 en hostelería Levante, 4.619 en agencias España. Es el único canal que da la persona, no sólo la empresa |
| D-15 | **Google Maps sale del ciclo semanal** y queda como recurso puntual | 22/08/2026 | Ya no aporta nada que la otra fuente no dé mejor, salvo la enumeración exhaustiva de negocios físicos de una ciudad. Cuándo volver a usarlo: `APIFY.md` §10 |
| D-16 | **Apify se queda como motor de enriquecimiento**, no de descubrimiento | 22/08/2026 | Contact Details, Website Content Crawler e Instagram. ≈0,89 $ por tanda de 20 leads |
| D-17 | **El email de invitación a reunión (`invitacion_reunion`) se envía automáticamente**, sin aprobación humana adicional, en cuanto el lead responde al primer contacto ya aprobado | 23/08/2026 | Única excepción a la regla 1 de `CLAUDE.md`, documentada allí explícitamente. El primer contacto sigue exigiendo aprobación humana sin excepción |
| D-18 | **La fecha de la reunión la confirma una persona** leyendo la respuesta del lead en el dashboard, no un parser automático de texto libre | 23/08/2026 | Evita agendar mal por una fecha ambigua ("el jueves que viene") o una zona horaria mal interpretada |
| D-19 | **Antes del primer contacto, llamada telefónica obligatoria** pidiendo permiso; se registra en `leads.permiso_llamada_en/por` y el botón "Contactar" no se activa sin ese registro | 23/08/2026 | Requisito legal según el negocio, más estricto que el diseño original (que se apoyaba sólo en interés legítimo). Refuerza la respuesta a P-03 |
| D-20 | **Despliegue en Vercel Hobby (gratuito)**, con el dominio `*.vercel.app` que da por defecto | 23/08/2026 | ⚠️ Vercel Hobby está documentado como **restringido a uso no comercial** (ver P-02 y `ARQUITECTURA.md` §7). Decisión tomada con esa advertencia sobre la mesa; revisar si Vercel lo cuestiona o pasar a Pro (20 $/mes) |
| D-21 | **Cada email debe indicar que lo envía un asistente de IA en nombre de Baladre**, y llevar un botón visible de baja | 23/08/2026 | Transparencia (AI Act) + refuerzo de RF-08. Actualizado en `CLAUDE.md` §6 |
| D-22 | **Piloto de envío por Gmail personal** (`nbrotonscongost@gmail.com`), sin dominio propio ni SPF/DKIM/DMARC, dirección intercambiable vía `GMAIL_USER`/`GMAIL_APP_PASSWORD` | 23/08/2026 | Respuesta interina a P-01. **Explícitamente para probar antes de entregar el proyecto al cliente** — la cuenta de Gmail de Nuria es de pruebas, no la definitiva. Se sustituirá por el buzón real de Baladre en la entrega, cambiando sólo esas dos variables de entorno. Verificación en dos pasos activada y contraseña de aplicación generada el 23/08/2026 — **probado con un envío real, funciona** |
| D-23 | **Cada tanda de prospección es de 10 leads** (bajo demanda o semanal), repartidos orientativamente 3 arquitectura / 3 agencias / 2 grupos tipo Vocento / 1 hotel / 1 restauración-joyería | 23/08/2026 | Confirmado explícitamente por Nuria. Reparto documentado en `SKILL.md`, no es una cuota rígida |
| D-24 | **P-05 resuelta: aprueba cualquier persona con acceso a la app**, sin roles diferenciados entre Eva y María del Mar | 23/08/2026 | Confirmado por Nuria. Lo que garantiza la trazabilidad no es un permiso granular, es el registro de auditoría (`aprobado_por`, `aprobado_at`), que ya existe en el esquema |
| ~~D-25~~ | ~~Despliegue en Vercel se deja para el final~~ | 23/08/2026 | **Superada por D-27 el mismo día**, minutos después: Nuria pide desplegar ya para poder hacer la primera prueba real |
| D-27 | **Se despliega en Vercel ahora**, no al final | 23/08/2026 | Sustituye a D-25. Motivo: hace falta una URL pública de `/api/ingest/leads` para que el motor pueda enviar la tanda de prueba sin depender de que el ordenador de Nuria esté encendido con `npm run dev` |
| D-28 | **Contraseña del primer usuario cambiada** por Nuria a una elegida por ella (ya no es la generada aleatoriamente) | 24/08/2026 | Sigue siendo `nbrotonscongost@gmail.com`, provisional hasta el usuario real de Baladre (P-05 histórica, ya resuelta por D-24 en cuanto a quién aprueba) |
| D-26 | **Zoom y Google Calendar del piloto usan la cuenta personal de Nuria** (`nbrotonscongost@gmail.com`), igual que el envío por Gmail (D-22) | 23/08/2026 | Confirmado por Nuria. Se sustituirá por las cuentas reales de Baladre en la entrega. Resuelve P-13/P-14 de forma interina — sigue pendiente crear la app de Zoom y el proyecto de Google Cloud sobre esa cuenta |
| D-29 | **P-15 (detección de respuesta) se resuelve con un cron de Vercel que llama a la API de Gmail directamente desde el servidor** (mismo patrón OAuth que Calendar/Zoom, con refresh token propio de scope `gmail.readonly`), **no con una "rutina" (tarea programada) de Claude** | 27/08/2026 | Comprobado en la práctica: las rutinas de Claude corren en un entorno de nube aislado que no hereda los conectores MCP personales (Gmail/Calendar/Drive) de la cuenta — la lista de conectores disponibles para rutinas salió vacía aunque el Gmail piloto está conectado y funciona en las sesiones normales de Claude Code. Probablemente una restricción deliberada (una rutina desatendida no debería tener acceso de lectura a una bandeja personal). Excepción acotada a D-04: esto es "detectar respuesta a un email ya enviado", no "buscar leads" — el motor de descubrimiento sigue viviendo en Claude. Nuria confirmó "haz lo que consideres mejor" ante este bloqueo |
| D-31 | **La tarea programada semanal del motor se sustituye por una cola de solicitudes** (tabla `solicitudes_prospeccion`) con dos formas de pedir una tanda desde la app: "prospectar ahora" o "programar para una fecha que yo elija" — nunca un cron con fecha fija | 27/08/2026 | A petición explícita de Nuria: no quiere una fecha fija semanal, prefiere decidir ella cuándo. Página `/campanas` con los dos botones/formularios; la skill `prospeccion-baladre` revisa la cola al arrancar (nueva sección "De dónde sale el número de leads y cuándo ejecutar" en `SKILL.md`) y nunca prospecta por iniciativa propia |
| D-32 | **Confirmado que la ejecución de la solicitud sigue exigiendo que una persona la pida desde Claude Code — no se automatiza del todo, ni con una rutina de Claude ni con una cuenta propia de Explorium** | 27/08/2026 | Nuria pidió expresamente automatizarlo del todo. Investigado a fondo: (1) las rutinas de Claude no pueden usar el conector de `vibe-prospecting` — de hecho **ningún** conector de los que aparecen en `claude.ai/customize/connectors` (Gmail, Supabase, etc.) es adjuntable a una rutina hoy, sólo Bash/Read/Write/Edit/Glob/Grep están garantizados; (2) los créditos de `vibe-prospecting` viven dentro del propio marketplace de conectores de Claude (`partner_id: "default_mcp"`, "platform key" según `show-pricing-plans`), no en una cuenta personal — sacar una clave directa en `admin.explorium.ai` sería una **relación nueva y separada** con Explorium, con plan gratuito limitado a enriquecer 500 registros/mes (no cubre claramente el descubrimiento/búsqueda que hace falta) y sin compartir el saldo ya pagado. Alternativa real descartada por Nuria: sustituir `vibe-prospecting` por Apify/Google Maps en las ejecuciones automáticas — habría revertido D-14/D-15 (peor calidad, sin decisor claro) sólo para ganar automatismo. **Decisión: se deja como está** — el botón/formulario de `/campanas` sólo encola la solicitud; sigue haciendo falta abrir Claude Code y pedir que la revise. Ver semáforo §1 |
| D-30 | **D-29 se implementa por IMAP con la contraseña de aplicación ya existente, no con la API de Gmail vía OAuth** | 27/08/2026 | Al intentar habilitar la Gmail API en el proyecto de Google Cloud `baladre-prospeccion-piloto`, Google exigió vincular una cuenta de facturación (pantalla de "prueba gratuita" pidiendo tarjeta) para poder seguir — aunque el texto de Google aclara que no se cobra nada a menos que se active manualmente una cuenta de pago completa. Nuria no quería dar una tarjeta para esto. Alternativa mejor y más simple: leer el correo por **IMAP** (`imap.gmail.com`) usando `GMAIL_USER`/`GMAIL_APP_PASSWORD`, las mismas credenciales que ya usa el envío por SMTP (D-22) — cero pasos nuevos en Google Cloud, cero facturación. Se cambia `lib/gmail.ts` (antes OAuth + API REST) por `imapflow` + `mailparser` (dependencias nuevas, justificadas aquí). Se borra la skill `.claude/skills/deteccion-respuestas-baladre/` creada en el intento anterior (basada en una rutina de Claude con MCP de Gmail) por quedar obsoleta |

## 3. Preguntas abiertas — bloquean trabajo

| # | Pregunta | Bloquea | Quién decide |
|---|---|---|---|
| P-01 | **Dominio de envío.** Baladre usa Gmail gratuito. ¿Se envía desde `baladreceramica.com` (hay que configurar SPF/DKIM/DMARC) o desde un subdominio tipo `hola.baladreceramica.com`? Respuesta interina de piloto (D-22): Gmail personal, sin dominio propio configurado. La pregunta de fondo para producción sigue abierta | Fase 4 | Nuria + Baladre |
| P-02 | **Hosting definitivo.** Vercel Hobby está restringido a uso no comercial (verificado en su documentación). ¿Se paga Pro a 20 $/mes o se busca alternativa (Railway, Render, Supabase Edge Functions)? | Despliegue | Nuria |
| P-03 | **Revisión jurídica LSSI art. 21** para el email B2B en frío | Fase 4 completa | Abogado |
| P-04 | **Directorios sectoriales**: cuáles permiten extracción automatizada según sus términos de uso | Alcance del motor | Nuria, leyendo términos |
| ~~P-05~~ | ~~Quién aprueba de verdad~~ | **RESUELTA el 23/08/2026**: cualquier persona con acceso a la app. Ver D-24 |
| ~~P-06~~ | ~~Migración del Sheets~~ | **RESUELTA el 23/08/2026**: los datos de `BALADRE_LEADS` son ficticios (de pruebas), no hay nada real que migrar. No se construye ningún script de migración |
| P-07 | **Límite de envíos diarios** una vez arranque la fase 4 | Fase 4 | Nuria |
| P-08 | **LinkedIn.** Nuria lo pidió; yo lo he dejado fuera porque sus condiciones prohíben la extracción automatizada y el riesgo recae sobre una cuenta real. Decidir entre: no usarlo, Sales Navigator de pago, o consulta manual persona a persona. **27/08/2026: por esto mismo se descartó conectar el Prospector de Relevance AI a este sistema** (usa Crustdata para LinkedIn) — ver registro de sesión de esa fecha | Cobertura de decisores | Nuria + Baladre, con criterio jurídico |
| P-09 | **Plan de Apify.** El Free (5 $/mes de crédito) cubre las pruebas; el ritmo semanal estimado se queda justo por encima. ¿Se sube a Starter (29 $/mes) o se recorta Instagram? | Coste mensual | Nuria, tras medir la primera tanda |
| P-10 | **MCP de Apify**: ¿funciona bien como herramienta desde Claude o se llama por API HTTP? No lo he verificado | Implementación del motor | Comprobar en la primera sesión del motor |
| ~~P-11~~ | ~~Conector de `vibe-prospecting` sin instalar~~ | **RESUELTA el 22/08/2026**: conectado y validado. Ver D-14 |
| P-12 | **Coste real de `vibe-prospecting`.** Confirmado que un fetch de 5 empresas costó 5 créditos (1 por fila) y que el enriquecimiento se cobra aparte, pero no he verificado el precio del crédito ni el plan contratado. Consultar `show-pricing-plans` antes de programar la tarea semanal | Presupuesto mensual y tamaño de la tanda | Nuria |
| ~~P-13~~ | ~~Cuenta y credenciales de Zoom~~ | **RESUELTA el 24/08/2026**: app "Baladre Reuniones" (Server-to-Server OAuth) creada, activada y probada sobre `nbrotonscongost@gmail.com` (D-26). Credenciales en `.env.local` |
| ~~P-14~~ | ~~Proyecto de Google Cloud + OAuth para Google Calendar~~ | **RESUELTA el 24/08/2026**: proyecto `baladre-prospeccion-piloto`, cliente OAuth web (con redirect al Playground), refresh token obtenido y probado sobre `nbrotonscongost@gmail.com` (D-26). Credenciales en `.env.local` |
| P-15 | **Conexión de la bandeja de Gmail de Baladre** para detectar respuestas automáticamente (RF ampliada: detección de respuestas ya no es "fase 2", se necesita ahora para disparar el email de invitación a reunión, D-17). **27/08/2026: código escrito (D-29/D-30, por IMAP), sólo falta comprobar que IMAP está habilitado en Gmail y fijar `CRON_SECRET`**, ver §6 | Motor de detección de respuestas | Nuria (comprobar ajuste de Gmail) |

## 4. Credenciales y cuentas necesarias

| Servicio | Estado | Coste verificado (21/08/2026) |
|---|---|---|
| Supabase (proyecto nuevo) | ✅ Creado y con esquema aplicado (`ytqjhsbcnswshybhgevc`) | Free: 500 MB, se pausa tras 1 semana sin actividad. Pro desde 25 $/mes |
| Vercel (equipo) | ⬜ | Hobby gratis pero **no comercial**; Pro 20 $/mes |
| Apify (token de API) | ✅ Token guardado en `.env.local` (`APIFY_API_TOKEN`) | Free: 5 $/mes de crédito. Starter 29 $/mes |
| Vibe Prospecting | ✅ Conector conectado y validado el 22/08/2026 | Créditos: 1 por fila en el fetch; enriquecimiento aparte. Precio del crédito sin verificar (P-12) |
| Resend (fase 4) | ⬜ | Free: 3.000 emails/mes, 100/día, 3 dominios |
| Dominio para envío | ⬜ | Depende de P-01 |
| GitHub (repo privado) | ✅ Repo creado y conectado (`github.com/Beniaia/REPOSITORIO-PRUEBA`) | Gratis |
| Zoom (API, OAuth/Server-to-Server) | ✅ App "Baladre Reuniones" creada, activada y probada el 24/08/2026 (token obtenido con scope `meeting:write:meeting:admin`). **26/08/2026: variables añadidas también al entorno de producción de Vercel** (sólo estaban en `.env.local`) | Gratis (cuenta básica de Zoom) |
| Google Cloud / Calendar API | ✅ Proyecto `baladre-prospeccion-piloto`, cliente OAuth web, refresh token obtenido y probado el 24/08/2026 (creación y borrado de un evento real). **26/08/2026: variables añadidas también al entorno de producción de Vercel** | Gratis dentro de cuota |
| Gmail de Baladre (lectura de respuestas) | ⬜ Pendiente (P-15) | Ya hay MCP de Gmail conectado a nivel de cuenta de Claude; falta confirmar que lee la bandeja correcta de Baladre |
| Gmail (SMTP piloto, envío real) | ✅ Verificación en dos pasos activada, contraseña de aplicación generada y probada con un envío real | Gratis dentro del límite de envío de Gmail (~500/día en cuenta personal) |

Todas las claves van a `.env.local` y a las variables de entorno de Vercel. **Ninguna al repositorio.**

## 5. Riesgos vivos

- **Alucinación de datos.** Es el riesgo que más puede dañar la marca de Baladre: un email a un arquitecto citando un premio que no ganó. Mitigación en el diseño (sin URL no hay señal, sin señal no hay email), pero hay que comprobarlo con lotes reales antes de confiar.
- **Supabase Free se pausa** tras una semana sin actividad. Si la tarea semanal falla dos veces seguidas, el proyecto puede quedarse dormido. Vigilar en las primeras semanas.
- **Coste de tokens del motor** no medido todavía. Hasta que no haya una ejecución real no sabemos el coste por lead. Registrarlo en `ejecuciones.coste_estimado` desde el primer día.
- **Convivencia con el sistema antiguo.** Mientras Relevance AI siga activo, hay dos sitios donde se puede escribir a un lead. Riesgo real de escribir dos veces a la misma persona. Fijar fecha de apagado del sistema anterior.
- **Envío por Gmail personal sin dominio propio (D-22).** Sin SPF/DKIM/DMARC alineados con `baladreceramica.com`, puede caer en spam con destinatarios de política estricta, y Gmail tiene un límite diario de envío (~500). Aceptable para el piloto; no vale para escalar volumen — ahí es donde vuelve a hacer falta resolver P-01 de verdad.
- **I/O de disco muy lento en este Mac (visto el 25/08/2026).** `npm run typecheck` tardó ~8 min (434 de 463 s fueron sólo lectura de disco) y `npm run build` ~35 min, sin ningún error de código de por medio. Vigilar si se repite; si persiste, revisar espacio libre en disco o si la carpeta del proyecto está sincronizándose con iCloud.

## 6. Siguiente paso concreto

1. ~~Rellenar `.env.local`~~ ✅ hecho el 24/08/2026 — las 8 variables están puestas, en local y en Vercel.
2. ~~Crear al menos un usuario en Supabase Auth~~ ✅ hecho el 23/08/2026, contraseña definitiva puesta por Nuria el 24/08/2026 (D-28).
3. ~~Responder P-05~~ ✅ resuelta (D-24).
4. ~~Desplegar en Vercel~~ ✅ hecho el 24/08/2026 — `repositorio-prueba-ylar.vercel.app`.
5. ~~Migrar el Sheets~~ ✅ no hacía falta (P-06 resuelta).
6. ~~Validar los esquemas de entrada de Apify~~ ✅ hecho el 23/08/2026 (se corrigió un error real).
7. **Resolver P-12**: coste por crédito de `vibe-prospecting` — parcialmente resuelto (precios de los paquetes conocidos, ver `VIBE_PROSPECTING.md` §6), falta saber el plan/saldo actual.
8. ~~Resolver P-13/P-14~~ ✅ hechas el 24/08/2026 (Zoom y Google Calendar creados y probados con llamadas reales). **P-15 (detectar la respuesta del lead) sigue sin resolver.**
9. **Flujo de reunión (Zoom + Calendar) — CERRADO el 26/08/2026.** Formulario añadido, `build`/`typecheck` en verde, y probado de punta a punta en producción con el lead `nuritina10@hotmail.com` (ver semáforo §1). Sólo queda:
   - d) Decidir cómo se resuelve P-15 (detección automática de respuesta): la opción que mejor encaja con la arquitectura (D-04, "el motor vive en Claude") es una tarea programada de Claude que lea el Gmail de Baladre con el MCP ya conectado y escriba en Supabase — evita montar IMAP y una cron de Vercel (que en el plan Hobby sólo puede correr una vez al día). Sin decidir todavía, para hablarlo con Nuria.
   - e) (Menor, no bloqueante) Limpiar en Zoom/Google Calendar la reunión y el evento "huérfanos" del primer envío duplicado del 26/08/2026 (`zoom_meeting_id` antiguo `72082838713`, `calendar_event_id` antiguo `ch4qq8ln9f3fef65jq1h0cmfb8`) — quedaron creados de verdad pero desvinculados en Supabase tras corregir el bug de doble envío.
10. **Ejecutar la skill del motor en modo prueba con 10 leads de Levante.** Nuria ha pedido explícitamente dejarlo para el final y seguir probando con leads ficticios mientras tanto (confirmado 27/08/2026).
11. Pendiente menor: hay dos proyectos de Vercel casi idénticos — ya resuelto, se borró `repositorio-prueba` el 24/08/2026, sólo queda `repositorio-prueba-ylar`.
12. **Terminar P-15 (27/08/2026, revisado el mismo día — D-30, IMAP en vez de la API de Gmail):**
    - a) Comprobar en la cuenta de Gmail piloto (`nbrotonscongost@gmail.com`) que el acceso IMAP está habilitado: Gmail → Configuración → Reenvío y correo POP/IMAP → "Habilitar IMAP". Con la verificación en dos pasos activa (ya lo está, D-22), la misma contraseña de aplicación que usa el envío sirve también para IMAP — no hace falta generar otra.
    - b) Generar un `CRON_SECRET` (cualquier cadena aleatoria) y añadirlo a `.env.local` y a las variables de entorno de Vercel — Vercel lo manda automáticamente como `Authorization: Bearer` al llamar al cron.
    - c) Redesplegar y comprobar en los logs de Vercel que el cron `/api/cron/deteccion-respuestas` corre a las 07:00 UTC y no da error.
    - d) Probar de punta a punta con el lead `nuritina10@hotmail.com` u otro de prueba: enviar `email_1`, responder desde otra cuenta, esperar al cron (o dispararlo a mano) y comprobar que `mensajes.respondido_en` se rellena solo.
13. **Diseño pendiente, sin construir todavía:** para cuando se retome la tarea programada semanal del motor (punto 3 de más arriba), Nuria prefiere dos botones en la app en vez de una fecha fija — "ejecutar 10 leads ahora" y "programar para cuando yo quiera" — en lugar de un cron con fecha rígida. Encaja como: la app escribe una solicitud en Supabase, algo la recoge y ejecuta. Sin diseñar en detalle ni implementado.

### Lo que ya funciona hoy (24/08/2026)

- Esquema de Supabase aplicado y verificado (`npm run build`/`lint`/`typecheck` en verde).
- App **desplegada y en producción**: `repositorio-prueba-ylar.vercel.app`, con las 8 variables de entorno configuradas (tras un primer 500 por falta de ellas, diagnosticado con `get_runtime_errors` del MCP de Vercel y corregido).
- `/login` con usuario real (`nbrotonscongost@gmail.com`, contraseña puesta por Nuria), `/baja`, `/leads` (bandeja) y `/leads/[id]` (ficha con permiso de llamada, aprobar/rechazar mensaje y botón "Contactar" gateado).
- `/api/ingest/leads`: valida token + esquema Zod, deduplica, consulta bajas, crea sólo borradores.
- Guardarraíles de envío escritos como triggers en la propia base de datos: no se puede marcar `enviado` sin aprobación (salvo la excepción `invitacion_reunion`), sin permiso de llamada (para `email_1`), ni si el contacto está en `bajas`.

## 7. Registro de sesiones

| Fecha | Qué se hizo | Quién |
|---|---|---|
| 21/08/2026 | Lectura de la presentación estratégica, definición de arquitectura, PRD, CLAUDE.md, esquema SQL y prompt maestro. Verificación de precios de Apify, Resend, Supabase y Vercel | Nuria + Claude |
| 21/08/2026 | Apify pasa a canal principal de descubrimiento: playbook `APIFY.md` con cuatro actores, recetas por segmento y estimación de coste. LinkedIn queda fuera del pipeline (D-11, P-08) | Nuria + Claude |
| 21/08/2026 | Prompt maestro v2, orientado a ahorro de tokens: prompts cortos por hito, reglas de economía de contexto en `APIFY.md` §9 y `CLAUDE.md` §11. Verificado que la API de Apify admite `fields`, `clean` y `limit` | Nuria + Claude |
| 21/08/2026 | Revisión del papel de Google Maps: se limita a hoteles y restauración (D-13). Al intentar probar `vibe-prospecting` se descubre que su conector no está instalado (P-11) | Nuria + Claude |
| 22/08/2026 | Conector de `vibe-prospecting` conectado. **Validación empírica de la cobertura** con consultas reales al ICP de Baladre; documento `VIBE_PROSPECTING.md` con recetas, universos medidos y tres trampas técnicas. Maps sale del ciclo semanal (D-14, D-15, D-16) | Nuria + Claude |
| 23/08/2026 | Repo conectado a GitHub y estructura inicial subida. Ampliación de alcance: seguimiento por respuesta con reunión de Zoom + Google Calendar, permiso telefónico obligatorio, transparencia IA en el email (D-17 a D-21, CLAUDE.md actualizado). Esquema v1.1 aplicado en un proyecto de Supabase ya existente (`ytqjhsbcnswshybhgevc`): tabla `reuniones`, permiso de llamada, triggers de guardarraíles. App Next 16 + React 19 + Tailwind scaffolded (login, bandeja, ficha de lead, `/baja`, endpoint de ingesta); `build`/`lint`/`typecheck` en verde y probado en local. Quedan abiertas P-13/14/15 (Zoom, Calendar, Gmail) y rellenar `SUPABASE_SERVICE_ROLE_KEY` | Nuria + Claude |
| 23/08/2026 | Botón "Contactar" conectado a un envío real por SMTP de Gmail (`lib/email/enviar.ts`, D-22): piloto con el correo personal de Nuria, sin dominio propio. Se reescribe `marcarContactado` para actualizar primero la fila en `mensajes` (donde disparan los guardarraíles de la BD) y sólo enviar el email si esa actualización tiene éxito de verdad; si el envío falla después, el mensaje pasa a `estado='error'` y se puede reintentar desde la ficha. Pendiente de que el usuario active la verificación en dos pasos y genere la contraseña de aplicación de Gmail | Nuria + Claude |
| 23/08/2026 | Primer usuario de Supabase Auth creado (`nbrotonscongost@gmail.com`), insertado directamente vía SQL (`auth.users` + `auth.identities`) con el MCP de Supabase, email confirmado. Contraseña temporal generada y entregada a Nuria en el chat — pendiente de cambiar y de sustituir por el usuario real de Baladre (Eva/María del Mar, P-05) | Nuria + Claude |
| 23/08/2026 | P-05 resuelta (D-24: aprueba quien tenga acceso a la app), despliegue en Vercel pospuesto al final (D-25), Zoom/Calendar interinos sobre la cuenta personal de Nuria (D-26). **Esquemas de entrada de los 3 actores de Apify validados** contra la API real (sin entrar en la consola, con el token ya guardado): se encontró y corrigió un error real en `APIFY.md` — Website Content Crawler usaba nombres de campo que pertenecen a Contact Details Scraper. Consultados los precios reales de `vibe-prospecting` (`show-pricing-plans`) para avanzar P-12 | Nuria + Claude |
| 24/08/2026 | **P-13 y P-14 resueltas.** App Zoom "Baladre Reuniones" (Server-to-Server OAuth) creada, activada (con el scope `meeting:write:meeting:admin`) y probada con un token real. Proyecto de Google Cloud `baladre-prospeccion-piloto` creado sin necesidad de la prueba gratuita de pago; Calendar API habilitada; cliente OAuth de tipo "Aplicación web" (el de "App de escritorio" no servía, daba `redirect_uri_mismatch` con el OAuth Playground — se borró); refresh token obtenido vía OAuth Playground y probado creando y borrando un evento real en el calendario de Nuria. Las 6 credenciales (Zoom + Google) están en `.env.local`. | Nuria + Claude |
| 24/08/2026 | D-25 revertida por D-27: se despliega ya, no al final. `.env.local` completado (`INGEST_SERVICE_TOKEN` generado, `SUPABASE_SERVICE_ROLE_KEY` obtenida del dashboard por Nuria). Contraseña del primer usuario cambiada por Nuria (D-28). Intento de conectar Vercel al repo vía MCP bloqueado primero por permisos, luego por falta de acceso de la app de GitHub al repo — Nuria completó el despliegue manualmente desde el propio panel de Vercel (`repositorio-prueba-ylar`, equipo `nuria-pruebas`). Primer despliegue daba **error 500** por falta de variables de entorno; diagnosticado con `get_runtime_errors` del MCP de Vercel (`Your project's URL and Key are required to create a Supabase client!`, roto en `/middleware`). Nuria añadió las 8 variables en el panel de Vercel; redeploy lanzado con un commit de este mismo archivo para forzarlo vía git. Guía paso a paso entregada para Zoom (Server-to-Server OAuth) y Google Calendar (proyecto de Google Cloud), ambos interinos sobre la cuenta personal de Nuria | Nuria + Claude |
| 24/08/2026 | Creados 5 leads de prueba (4 ficticios + 1 con el email real de Nuria, `nuritina10@hotmail.com`) vía el endpoint de ingesta, para probar la bandeja y, más adelante, el envío real. Proyecto duplicado de Vercel (`repositorio-prueba`) borrado por Nuria. **Empezado el código del flujo "responde el lead → reunión automática"**: migración de BD (`respuesta_extracto`, `respuesta_de` en `mensajes`, v1.2 del esquema), `lib/zoom.ts`, `lib/google-calendar.ts`, `lib/fecha-madrid.ts`, y las acciones `registrarRespuestaLead`/`confirmarFechaReunion` en `lib/actions/leads.ts`. **Sesión cortada antes de añadir el formulario en la ficha del lead y antes de compilar o probar nada** — este código no es usable todavía. P-15 (detección automática de respuesta) sigue sin resolver; la opción que mejor encaja con la arquitectura es una tarea programada de Claude leyendo el Gmail de Baladre por MCP, en vez de IMAP + cron de Vercel (que en el plan Hobby sólo puede correr una vez al día) — sin decidir, pendiente de hablarlo con Nuria | Nuria + Claude |
| 25/08/2026 | Añadido el formulario de reunión en la ficha del lead (`app/(app)/leads/[id]/page.tsx`): registrar respuesta del lead y confirmar fecha, conectado a `registrarRespuestaLead`/`confirmarFechaReunion`. `npm run typecheck` y `npm run build` verificados en verde — ambos anormalmente lentos (~8 y ~35 min) por I/O de disco de este Mac, no por el código (ver Riesgos §5). Intento de probar el flujo real de punta a punta con el lead `nuritina10@hotmail.com`: descartado el enlace mágico de Supabase para loguear sin contraseña (la app no tiene una ruta cliente que procese el token, no se estableció sesión); un script que reproducía la lógica real de `confirmarFechaReunion` llamando directamente a `lib/zoom.ts`/`lib/google-calendar.ts`/`lib/email/enviar.ts` fue bloqueado dos veces por el clasificador de Auto Mode al implicar envíos y creaciones reales, igual que un intento de resetear la contraseña por la API de administración de Supabase. Se optó por que Nuria hiciera la prueba manualmente desde la app; al intentarlo, la contraseña de producción de `nbrotonscongost@gmail.com` no fue aceptada — Nuria ha iniciado un reseteo por email de recuperación desde el panel de Supabase. **Prueba real sin terminar**, pendiente para la próxima sesión (ver Siguiente paso §6, punto 9c) | Nuria + Claude |
| 26/08/2026 | Añadido un botón para **eliminar un lead de forma permanente**, a petición de Nuria: sección "Zona de peligro" al final de la ficha del lead (`app/(app)/leads/[id]/page.tsx`), discreta y en rojo, con confirmación del navegador antes de borrar (`BotonBorrarLead.tsx`, único componente cliente nuevo). La acción `borrarLead` (`lib/actions/leads.ts`) audita antes de borrar y sólo elimina la fila de `leads` — sus emails y su reunión se borran en cascada (ya estaba así en el esquema), pero la empresa y el contacto no se tocan | Nuria + Claude |
| 26/08/2026 | Añadido el filtro **"Tipo de cliente"** (segmento: arquitectura/interiorismo, agencia/eventos, grupo de comunicación, hotel premium, restauración, joyería/retail) a la bandeja, a petición de Nuria, junto a los filtros ya existentes | Nuria + Claude |
| 26/08/2026 | Añadidos filtros a la bandeja (`/leads`), a petición de Nuria: nivel, permiso de llamada, mail enviado, reunión Zoom y bajas, cada uno como un desplegable sencillo (sin JavaScript, vía parámetros de la URL) con un contador de resultados arriba de la tabla — así se puede ver de un vistazo, por ejemplo, "todas las reuniones programadas" o "cuántos leads tienen el permiso de llamada confirmado" | Nuria + Claude |
| 26/08/2026 | Añadidas 3 columnas a la bandeja (`/leads`), a petición de Nuria: **Mail enviado** (estado del `email_1`), **Reunión Zoom** (estado de la reunión: programada/rechazada/realizada/propuesta) y **Bajas** (en rojo "No contactar" si el contacto está en la lista de bajas). Migración `v_bandeja_email_y_reunion` aplicada en Supabase para exponer `email_1_estado` y `reunion_estado` en la vista `v_bandeja`; `schema.sql` sincronizado | Nuria + Claude |
| 27/08/2026 | **P-15 (detección automática de respuesta): código escrito, sin desplegar.** Refactor de `registrarRespuestaLead` para compartir lógica con la detección automática (`lib/respuestas/registrar-respuesta.ts`); endpoint `/api/deteccion-respuestas` (GET pendientes, POST registrar). Intento de resolverlo con una **rutina de Claude** (tarea programada en la nube): bloqueado en la práctica — las rutinas no tienen acceso a los conectores MCP personales (Gmail/Calendar/Drive) de la cuenta, confirmado consultando dos veces el listado de conectores disponibles para rutinas (vacío) e inspeccionando la página de conectores de Nuria en su navegador real. Decisión D-29: se resuelve en su lugar con un **cron de Vercel** (`vercel.json`, diario 07:00 UTC). Primer intento de `lib/gmail.ts` con la API de Gmail vía OAuth — bloqueado también: al habilitar la Gmail API en el proyecto de Google Cloud, Google exigió vincular una cuenta de facturación con tarjeta (pantalla de "prueba gratuita"), y Nuria no quería darla. **Decisión D-30**: se reescribe `lib/gmail.ts` para leer por **IMAP** (`imapflow` + `mailparser`, dependencias nuevas) con la misma contraseña de aplicación que ya usa el envío por SMTP — cero pasos en Google Cloud, cero facturación. Se borra la skill `deteccion-respuestas-baladre` (del intento con rutina de Claude, ya obsoleta). `npm run typecheck` en verde tras el primer bloque de cambios (antes del giro a IMAP). Queda pendiente (§6 punto 12): comprobar que IMAP está habilitado en Gmail, generar `CRON_SECRET`, desplegar y probar en real. Aparte, Nuria confirmó explícitamente: dejar la primera tanda real de prospección para el final (seguir con leads ficticios), y para la futura tarea programada semanal del motor prefiere un botón "ejecutar ahora" + poder programar ella misma la fecha, en vez de una fecha fija (sin construir todavía, anotado en §6 punto 13) | Nuria + Claude |
| 27/08/2026 | **P-15 desplegado en producción** (commit `028fab1`, push a GitHub, redeploy en Vercel confirmado `READY` sin errores de runtime). `CRON_SECRET` añadido a `.env.local` y a Vercel por Nuria; comprobado que IMAP ya estaba habilitado en la cuenta piloto de Gmail. Prueba directa del endpoint del cron bloqueada por el clasificador de Auto Mode (llamada real a producción con token) — pendiente de decidir con Nuria cómo verificarlo (esperar al cron real de las 07:00 UTC, o autorizar la llamada de prueba). **Añadida la página `/campanas`** (D-31): tabla nueva `solicitudes_prospeccion` (migración aplicada en Supabase, `schema.sql` sincronizado), botón "Prospectar 10 leads ahora", formulario "Programar para cuando yo quiera" (fecha + hora + nº de leads), lista de solicitudes pendientes con opción de cancelar, e historial de `ejecuciones`. La skill `prospeccion-baladre` se actualiza para consultar esa cola al arrancar (usa el `numero_leads` de la solicitud, la marca `en_proceso` y luego `completada`) y para no prospectar nunca por iniciativa propia. Queda sin resolver quién dispara la skill cuando hay una solicitud — sigue haciendo falta una persona con Claude Code abierto | Nuria + Claude |
| 27/08/2026 | **Cerradas dos vías más para automatizar `/campanas` del todo, ambas descartadas por Nuria.** (1) Conectar el agente Prospector de Relevance AI (sistema anterior, D-01): descartado sin diseñar nada porque ese Prospector usa Crustdata para buscar perfiles de LinkedIn, lo que choca con D-11 ("LinkedIn fuera del pipeline") y con P-08, sin resolver. (2) Sacar una clave de API propia de Explorium (el proveedor detrás de `vibe-prospecting`) para llamar al descubrimiento por `curl` sin sesión de Claude: Nuria se registró gratis en `admin.explorium.ai` y compartió la clave; se probó el endpoint real `POST /v1/businesses` en modo `preview` (gratis) y `full` (gastó 3 créditos reales de 100) filtrando por España — **la cuenta de tipo "trial" devolvió siempre las mismas 5 empresas de ejemplo (Google, Amazon, LinkedIn, Microsoft, Deloitte), ignorando el filtro, incluso cobrando créditos reales**. No sirve para descubrir leads de verdad sin contratar un plan de pago propio en Explorium, ajeno a lo ya pagado en `vibeprospecting.ai`. Nuria decidió no seguir por ninguna de las dos vías: **D-32 se mantiene** — la ejecución de una solicitud de `/campanas` sigue exigiendo pedirlo desde Claude Code | Nuria + Claude |
| 27/08/2026 | **Investigada a fondo la automatización total de `/campanas` (D-32).** Nuria pidió que "prospectar ahora" se ejecutara solo, sin abrir Claude Code. Comprobado: las rutinas de Claude no tienen ningún conector adjuntable hoy (ni Gmail, ni Supabase, ni nada de lo que aparece en `claude.ai/customize/connectors`), sólo Bash/Read/Write/Edit/Glob/Grep; los créditos de `vibe-prospecting` viven dentro del marketplace de conectores de Claude (`partner_id: "default_mcp"`, confirmado con `show-pricing-plans`), no en una cuenta personal de Explorium — una clave directa en `admin.explorium.ai` sería una relación nueva, con plan gratuito limitado a enriquecimiento (no descubrimiento) y sin compartir el saldo ya pagado. Alternativa de usar Apify/Google Maps para las ejecuciones automáticas (revirtiendo D-14/D-15) descartada por Nuria por peor calidad. Decisión final: se deja como está — el botón sólo encola la solicitud, sigue haciendo falta pedirlo desde Claude Code | Nuria + Claude |
| 26/08/2026 | **Flujo de reunión probado de punta a punta en producción, con éxito.** El bloqueo de login era una contraseña equivocada de Nuria, no la app. Nuria hizo la prueba manualmente desde la ficha del lead `nuritina10@hotmail.com`: permiso de llamada → aprobar → Contactar (email real enviado) → registrar respuesta → confirmar fecha. Dos bugs reales encontrados y corregidos en el momento: (1) el campo combinado `datetime-local` no era usable en Chrome/macOS (no aceptaba ni texto ni el selector visual) — sustituido por dos inputs separados, fecha y hora; (2) al confirmar la fecha, faltaban las 6 variables de Zoom/Google Calendar en el entorno de producción de Vercel (sólo estaban en `.env.local`, nunca se habían copiado al añadir las 8 originales) — Nuria las añadió desde el panel de Vercel (con guía paso a paso muy detallada, incluyendo cómo encontrar el archivo `.env.local` oculto) y se redesplegó. Con eso, la reunión se confirmó de verdad: Zoom real, evento de Calendar real, invitación automática enviada. Se detectó que el formulario se había enviado dos veces (doble clic), creando una segunda reunión de Zoom, un segundo evento de Calendar y un segundo email real — corregido añadiendo una comprobación de idempotencia en `confirmarFechaReunion` (si la reunión ya está `confirmada`, no crea nada más), verificado con `typecheck` y desplegado. Queda pendiente, no bloqueante, borrar manualmente en Zoom/Calendar la reunión y el evento huérfanos del primer envío duplicado. **F1 (flujo de reunión) queda cerrada** salvo P-15 | Nuria + Claude |
